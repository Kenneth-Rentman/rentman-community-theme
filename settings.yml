load_manrope_webfont:
  type: bool
  default: true

use_gilroy_headings:
  type: bool
  default: false

gilroy_stylesheet_url:
  type: string
  default: ""

base_border_radius:
  type: integer
  default: 4
  min: 0
  max: 16

hero_primary_cta_label:
  type: string
  default: "Ask a question"

hero_primary_cta_url:
  type: string
  default: "/new-topic"

hero_secondary_cta_label:
  type: string
  default: "Visit the Support Center"

hero_secondary_cta_url:
  type: string
  default: "https://support.rentman.io"
